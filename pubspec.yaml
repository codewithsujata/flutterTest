name: expense_app
description: "A new Flutter project."

# Prevents accidental publishing to pub.dev. Remove if publishing.
publish_to: 'none'

# Defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  image_picker: ^0.8.7+4


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6

  # Add other dependencies here as needed.
  flutter_svg: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.0.13 
  # shared_preferences allows storing simple key-value data persistently, useful for saving user settings and small data across sessions.

  # The "flutter_lints" package contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0

flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/Splithublogo.svg
    - assets/secondImg.png
    - assets/friends.png
    - assets/invoice.png

  fonts:
  - family: InterTight
    fonts:
      - asset: assets/fonts/InterTight-Regular.ttf
      - asset: assets/fonts/InterTight-Bold.ttf
        weight: 700
      - asset: assets/fonts/InterTight-SemiBold.ttf
        weight: 600
      - asset: assets/fonts/InterTight-Medium.ttf
        weight: 500
      - asset: assets/fonts/InterTight-ExtraBold.ttf
        weight: 800

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
